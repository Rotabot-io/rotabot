version: "2"
sql:
  - engine: "postgresql"
    queries: "assets/queries/"
    schema: "assets/migrations/"
    gen:
      go:
        sql_package: "pgx/v5"
        package: "db"
        out: "lib/db"
        emit_json_tags: true
